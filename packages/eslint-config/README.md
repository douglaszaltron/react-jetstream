# @react-jetstream/eslint-config

## Installation

`npm i @react-jetstream/eslint-config --save-dev`

## Usage

On `.eslintrc.json` file add:

```json
{
  "extends": "@react-jetstream/eslint-config"
}
```
